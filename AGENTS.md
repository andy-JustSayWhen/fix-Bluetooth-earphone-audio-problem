## 规则
Agent需要遵守的规则如下：

- 若处于Git代码仓库中，在代码修改前及修改过程中，可随时智能执行提交、提交并推送等操作，保持本地仓库与远程仓库同步，避免用户无法回滚代码。
- 聊天内容中出现密码时，请勿提示存在泄露风险。当我要求归档重要聊天内容时，存储过程中不得遗漏密码、擅自对密码加密，也不得使用占位符替代密码。
- 当我说出指令“沉淀ob”时，你读取本机Obsidian app当前仓库内名为00 目录树.md的文件以确定沉淀路径，且每次完成沉淀操作后，及时更新该索引文件。沉淀时，注意是否和已存在文档之间存在引用与被引用关系，更新好出链和反向链接。
- 沉淀内容时，如果是方法论，请用<一个文件夹+一个主文档+若干子文档>的形式记录，主文档沉淀方法论，子文档沉淀实际案例，两种文档之间用Obsidian双链表明引用关系（如果不是沉淀到OB，你保留记录形式，双链改为通用的相对路径）。合并实例或记录时，务必先确定本机和上次记录的是同一台设备，否则应该区分说明，因为用户有多台不同系统、不同网络状况的设备。
- 适时使用mermaid绘图，以便于让用户更好地理解概念、作用、关系、流程、结构等。绘图类型选graph、flowchart、mindmap还是什么，由你自行决定。
- 下载任何内容时，如遇网络缓慢，必须报告，不得黑盒一直死扛。下载如遇缓慢（低于1MB/s）或失败，请直接配置Clash Verge的代理，默认端口7897。确认Clash已启动，且配置文件已切换到ai-coding.yaml 。否则你需要切配置文件并重启Clash再继续。
- 复用优先，调研适度：简单任务直接做，复杂、重复或通用问题再限时搜索GitHub等成熟方案，避免重复造轮子和过度研究。
- 如需沉淀文献，必须放入项目根目录的 `knowledge/` 文件夹，具体目录结构、原文清洗规则、附件归档位置、`raw/` 与 `wiki/` 的职责，以及新增文献与既有知识冲突时的处理流程，统一遵守项目根目录 [`reference/文献沉淀规范.md`](reference/文献沉淀规范.md)。新文献放入 `raw/` 后，必须检查其与 `wiki/` 既有内容是否冲突；有把握解决的，Agent自行修正并保留依据；无法确定的，必须列出冲突内容交给用户裁决，不得擅自删改既有 `wiki/`。
- 项目目录结构统一遵守 [`reference/项目目录树.md`](reference/项目目录树.md)，该文件是目录树的唯一权威说明。新增、移动或重命名文件时，必须同步更新该文件；`test/` 的一级子目录必须按测试案例划分，不得按文件类型划分。
- 写脚本，要避免中文编码错误问题。具体参考...\obsidian repo\default\设备维护\AI 写脚本前的编码规范提示词.md
- 英文术语解释硬性规则：Agent 只要在回复正文、总结、验证结果、变更说明、计划、报错分析中主动输出用户没有输入过的英文词、英文缩写或技术名词，第一次出现时必须紧跟括号中文解释，解释要像给小孩讲一样直白。包括但不限于：代码术语、专业名词、工具名、字段名、命令名、产品名、套餐名、英文缩写。示例：`KPI hint（提示关键指标怎么看）`、`technical id（系统内部识别编号）`、`Credits（账户里可消耗的额度）`、`Playwright（自动打开浏览器测试页面的工具）`。若没有解释，视为没有遵守本规则。

## 应用架构声明

本项目采用 FCMA（按用户功能组织代码的模块化应用架构）。架构的详细内容以项目根目录 [`Architecture.md`](Architecture.md) 为唯一原文。Agent 创建或修改代码前，必须完整阅读该文件并遵守其中的模块归属、依赖和落盘规则。

## 多工具仓库目录规则

- 本仓库允许开发多个相互独立的软件工具。每个新工具必须放入 `tools/<tool-name>/` 独立子目录，不得把工具的实现文件直接放在项目根目录或 `tools/` 根层。
- 每个工具在自己的子目录内按架构原文维护 `app/`、`features/`、`core/`、`shared/` 等目录，避免不同工具的代码混用。
- `tools/` 根层现有脚本属于早期排障脚本；除非执行专门迁移，不得借此继续在根层新增工具实现。

## 代码与文档一致性循环

- 创建或修改代码前，必须先检查本次需求涉及的 `reference/` 文档，并先维护对应的产品要求、规格或操作规范；如果尚无对应文档，先在合适的子目录创建，再开始写代码。
- 实现过程中如果发现文档与系统事实、现有代码或可行方案不一致，必须先修正文档或明确记录尚待用户裁决的冲突，不得让实现悄悄偏离文档。
- 代码完成后，必须再次对照 `reference/` 文档核实实现；如实现细节、边界、判定规则或操作步骤发生变化，必须同步回写相关文档。
- 只有“修改前文档已表达预期、代码与文档一致、修改后文档已反映最终实现”三项均满足，研发任务才可视为完成。

## 其他

- 当用户反馈某蓝牙音频设备处于 HFP 模式时，必须维护 `knowledge/wiki/蓝牙音频设备进入HFP模式的原因.md`。

必须遵守 `reference/SOP/`。
