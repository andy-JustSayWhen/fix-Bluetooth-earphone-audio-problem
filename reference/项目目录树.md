# 项目目录树

## 权威规则

本文件是项目目录结构的权威说明。新增、移动或重命名项目文件时，必须在同一次修改中同步更新本文件；如果其他说明与本文件冲突，以本文件为准。

## 当前维护结构

```text
.
├── AGENTS.md                         # Agent 工作规则
├── Architecture.md                   # 蓝牙音频模式检查器架构说明
├── README.md                         # 项目简介和入口说明
├── docs/                             # 项目排查记录和证据
│   ├── 2026-07-16-macos-audio-device-verification.md
│   ├── macos-audio-monitor.md
│   └── evidence/                     # 截图、日志和矩阵证据
├── knowledge/                        # 长期知识库
│   ├── assets/                       # 知识文档引用的附件
│   ├── raw/                          # 清洗后的原文资料
│   │   ├── apple/                    # Apple 官方或开发者资料
│   │   ├── bluetooth-sig/            # Bluetooth SIG 协议资料
│   │   ├── community/                # 社区工具、讨论和实践资料
│   │   └── microsoft/                # Microsoft 官方资料
│   └── wiki/                         # 可复用知识总结和案例
│       ├── cases/                    # 按日期和主题记录的实测案例
│       └── soundsource服务运维/       # SoundSource 服务停用、恢复和监测方法
├── reference/                        # 项目规范、交接和结构说明
│   ├── PROJECT_HANDOFF.md            # 当前排障状态与换机交接记录
│   ├── SOP/                          # 标准操作流程
│   ├── SPEC/                         # 工具行为、恢复流程和验证规格
│   ├── 文献沉淀规范.md
│   └── 项目目录树.md
├── tools/                            # 可复用排查工具和脚本
│   ├── bluetooth-audio-mode-checker/ # 本地网页化蓝牙音频模式检查器
│   ├── macos-audio-route-monitor.swift
│   └── run-macos-audio-monitor.sh
├── test/                             # 测试案例及其音频、输入、输出产物
│   ├── 2026-07-16-采样率与声道听感对照/ # 采样率、声道和听感对照案例
│   └── 混音-黑夜中/                   # 黑夜中音频混音工程及其测试产物
│       ├── 原始素材/                  # 混音使用的原始音频
│       ├── 工程文件/                  # 当前混音工程及工程依赖音频
│       └── 交付物/                    # 16 kHz、单声道、16 位最终音频
├── artifacts/                        # 运行时生成的临时结果，不作为源码结构
├── .build/                           # 编译生成目录，不作为源码结构
└── .codex/                           # Codex 运行环境和本项目本机备份
```

## `test/` 目录规则

- `test/` 的一级子目录按测试案例划分，不按文件类型划分；禁止建立 `test/audio/`、`test/wav/` 这类以文件类型为名的一级目录。
- 一个测试案例的原始音频、参考输入、转换产物和对照结果，统一放入同一个案例目录。
- 案例目录命名优先使用“日期-主题”，例如 `test/2026-07-16-采样率与声道听感对照/`。
- 默认不在案例内部继续按“原始文件/转换产物”等文件类型拆目录；只有测试阶段、设备或场景确实需要区分时，才建立有明确语义的子目录。
- 知识文档引用测试文件时，必须使用相对路径；移动测试文件后必须同步修正文档链接。
- 新增测试案例时，必须同时更新本文件，说明案例目录及其用途。

## `reference/SPEC/` 关键规格主题

- `HFP模式一键修复方案.md`：设备已判定为 HFP/HSP 后，一键修复的原因路由、处理动作、兜底和验收规则。
- `如何判定蓝牙音频设备的音频模式.md`：蓝牙音频设备模式判定、刷新监听和页面展示规则。
- `如何无扰动采集音量异常参数.md`：音量异常参数采集规则。
- `工具详细日志.md`：工具详细日志记录规则。

## `tools/bluetooth-audio-mode-checker/` 关键功能文件

- `features/a2dp-recovery/format-request-diagnosis.ts`：读取并解析最近系统声音日志，建立格式请求、进程和 `tsco` 的时序证据链。
- `features/a2dp-recovery/format-request-diagnosis.test.ts`：格式请求日志解析与证据分级测试。

## 生成目录规则

`artifacts/` 和 `.build/` 只保存运行或编译生成的临时结果，不用于存放手工维护的测试输入、知识文档或长期规则。需要长期保留的测试文件应放入对应的 `test/<测试案例>/`；需要长期引用的知识附件应放入 `knowledge/assets/`。

`.codex/` 保存 Codex 环境配置和本项目本机排障备份。此目录属于项目协作状态的一部分；新增可复用规范仍应放入 `reference/`。
