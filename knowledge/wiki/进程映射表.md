# 进程映射表

## 用途
在开发本应用时，我发现无法根据某些进程名称推断出其对应的App等，所以，专门创设本文档用于记录某些进程的归属App、触发者App和触发实例。
仅当用户要求新增记录时，AI才可以在总表内创设新条目，否则只能维护已有条目。
## don't do
除非用户要求，否则：
不得修改总表的列数及列名称；
各级标题即本文档的目录树、文章结构（章节），你不得删增。比如目前有3个二级标题##，你删除了其中一个，或者新增了第四个##，这些都是 不被允许的。
不得罔顾章节的格式、结构，在你需要修改或删增条目时，已有条目就是最好的格式要求和格式示例。


## 映射总表

| 进程名 | 归属 App | 触发者App |
| --- | --- | --- |
| `replayd` | macOS 系统采集服务 | 网易 UU远程通过该进程实现远程控制 macOS 设备 |

## 触发实例
（每个实例只能写一句话，在超过100字的前提下，尽量描述详细）

1. 2026-07-20 23:47，网易 UU远程在 `andymacbook-air` 传输画面和系统声音时调用 `replayd`，未发现其读取实体麦克风的证据。
